## Vue frontend using vue-applo

### install

if vue cli 3. vue appllo can be installed from vue cli plugin.

`vue add apollo` 

vue-applo-cli-plugin add client dependycies. Also add client example, and server example if you need.
